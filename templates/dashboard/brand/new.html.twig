{% extends 'dashboard/base.html.twig' %}

{% block title %}New Brand{% endblock %}

{% block body %}
    <h1>Create new Brand</h1>

    <div class="container">
        {{ include('dashboard/brand/_form.html.twig') }}
    </div>

    <a href="{{ path('dashboard_brand_index') }}">back to list</a>
{% endblock %}
